name: CI Pipeline
on:
  push:
    branches: [main]

jobs:
  build-slam-backend:
    name: Build SLAM API Server
    uses: bcgov/citz-imb/.github/workflows/Build.yml@696a755e065929ca5ba272c42b4d499771355300
    with:
      APP_NAME: slam-api
      APP_PATH: ./apps/slam/api
      PORT: 3333
