name: CICD

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
  pull_request:
    branches: [main]

env:
  runner: ubuntu-20.04

jobs:
  firstjob:
    runs-on: ubuntu-20.04
    strategy:
      fail-fast: true
      matrix:
        apps: ['slam-api', 'slam-app']

    steps:
      - run: echo Run ${{matrix.run}}